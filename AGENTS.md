# Development rules

- Use the minimum context needed; inspect targeted files rather than auditing the repository.
- Make the smallest coherent change for the current milestone and avoid unrelated refactors.
- Preserve accessibility and responsive behaviour.
- Avoid unnecessary dependencies and keep milestones small.
- Use targeted validation; distinguish regressions from pre-existing issues.
- Never create or commit `package-lock.json`.
