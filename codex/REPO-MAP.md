# Repository map

- `src/` — React UI and workspace/source application logic
- `src/types.ts` — shared frontend workspace and discovery types
- `src/App.tsx` — workspace and screenshot evidence library UI
- `src-tauri/src/` — Tauri commands, bounded filesystem scanning, and persistence
- `src-tauri/src/lib.rs` — OpenAI screenshot analysis boundary, evidence validation, and app-data evidence persistence
- `src-tauri/` — Tauri configuration and Rust dependencies
- `context/` — product concept and durable product context
- `codex/` — agent context and repository navigation
- `src-tauri/src/lib.rs` — focused Rust scanner and persistence tests
